12... Rad1: More important than the nominal evaluation is that the move creates an easier practical task through mobility (multiple regrouping squares) and forgiveness (more than one safe continuation).

The resulting task is comfortable. That matters because mobility (multiple regrouping squares) and forgiveness (more than one safe continuation) shape the workload.

The direct alternative stays secondary because the sharper "e4" push gives Black tactical counterplay for little gain.
