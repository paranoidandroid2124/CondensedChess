# Packet: P6-B01 shared-target continuity certification

## Scope

- phase anchor: `Phase 6/8`
- basis / surface: certification-owned shared-target continuity
- families in scope:
  - `FixedTargetComplex`
  - `RestrictionShell`
  - `CriticalSquareComplex`
  - `AccessNetwork` only where already certified by the exact target slice

## Goal

Close one shared-target continuity witness across `WhatMattersHere`,
`WhyThis`, and `WhatChanged` so the runtime can prove one continuing
fixed-weakness campaign without broadly reopening `TransitionBridge` or a
generic campaign-thread engine.

## Non-goals

- no broad `TransitionBridge` reopening
- no generic campaign-thread engine
- no new target-family admission outside the packet-owned exact continuity
  witness
- no legacy topology resurrection
- no raw board reopening below the canonical boundary

## Touched Files

- runtime:
  - certification / planner / renderer files only if the packet-owned exact
    continuity witness requires a narrow composite rule
- tests:
  - packet-owned shared-target continuity tests
- corpus:
  - packet-owned exact target-continuity expectations if needed
- docs:
  - `modules/llm/docs/StrategicObjectRoadmap.md`
  - `modules/llm/docs/StrategicObjectModel.md`
  - `.agents/packets/queue.md`
  - `.agents/README.md`
  - `modules/llm/docs/CommentaryTrustHardening.md` only if runtime trust meaning moves

## Exact Rows

- positive:
  - the exact comparative-support control from `P9-A01`
  - the exact target-fixation control from `P9-A02`
  - the exact current-position fixed-target probe control from `P9-A04`
- negative:
  - `K03A`
- near_miss:
  - `K09E`
- nasty_negative:
  - pressure-only or target-touch-only rows that must not become one
    continuing campaign

## Validation

- packet-owned shared-target continuity `sbt testOnly`
- relevant planner / shell regression `sbt testOnly`
- `sbt -batch "llm/compile"`

## Exit Criteria

- one shared-target continuity witness survives across `WhatMattersHere`,
  `WhyThis`, and `WhatChanged` without collapsing the three axes into one broad
  slogan
- unrelated target-touch or pressure rows remain fail-closed
- required doc sync lands in the same change if runtime meaning moved

## Review Focus

- scope creep risks: generic campaign-threading or broad `TransitionBridge`
  reopening
- boundary risks: collapsing three exact target slices into one unsliced shell
- legacy resurrection risks: old target-campaign salvage paths

## Certify Focus

- object admission: unchanged
- delta witness: each slice keeps its own local witness burden while shared
  target identity continuity is certified separately
- planner ownership: `WhatMattersHere`, `WhyThis`, and `WhatChanged` remain
  axis-correct while belonging to one continuing target campaign
- promotion / defer: no new family promotion; continuity proof only

## Status Notes

- start status: `ready`
- end status: `blocked`
- blocked on: `2026-04-10`
- primary blocker:
  - `sbt -batch "llm/testOnly lila.llm.strategicobject.ComparativeSupportAdmissionTest"`
    still failed during the packet attempt because the packet-owned
    `WhatChanged` support slice was not stable enough to keep the intended
    comparative support boundary fail-closed
- exact missing boundary:
  - the packet-owned `WhatChanged` support slice is not stable enough on the
    near-miss comparative row, so shared-target continuity cannot be certified
    without reopening a broader comparative-support boundary than this packet
    allows
  - `P9-R05` must not be opened yet because the unresolved burden still sits
    inside `P6-B01`
- pass condition:
  - one continuing fixed-weakness campaign survives across the three axes
    without broadening semantics
- blocked condition:
  - shared-target continuity still requires a broader exact boundary than this
    packet allows
